name: MyCustomCI

on:
  push:
    branches: [master]

jobs:
  build:
    steps:
      - name: printText
        run: echo "Hello CI!"

      - name: osTest
        run: pwd

      - name: ll
        run: ls -al

      - name: cat
        run: cat README.md
